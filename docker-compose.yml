embedly:
  build: ./embedly
  env_file: .env
  user: root
  expose:
    - "7001"
  links:
    - redis
    - statsd
  restart: never 

nginx:
  build: ./nginx
  links:
    - embedly
  ports:
    - "80:80"
  restart: never 

redis:
  image: redis
  restart: never

statsd:
  build: ./statsd
  tty: true
  expose:
    - "8125"
  ports:
    - "8125:8125"
